<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="shortcut icon" href="covid-19.png" type="image/x-icon">
    <title>Covid-API</title>
</head>

<body>
    <div class="bg"></div>
    <div class="content">
        <h1 class="title"><span>Covid</span>-API</h1>
        <form action="/data" method="POST">
            <input type="text" name="countryname" id="countryname" value="<% if (locals.name) { %>
             <%= name.name.toUpperCase() %>
            <% } %>" placeholder="Enter the Country Name" class="textbox">
            <button type="submit" class="btn">.</button>
        </form>
        <% if (locals.result) { %>
         <%- include('searchform.ejs') %>
        <% } %>
        <div class="display">
            <% if (locals.result) { %>
                <h1 class="result">Total Cases: <span>
                        <%= result.totalcases.toLocaleString() %>
                    </span></h1>
                <h1 class="result">Total Deaths: <span>
                        <%= result.totaldeaths.toLocaleString() %>
                    </span></h1>
                <h1 class="result">Recovered: <span>
                        <%= result.recovered.toLocaleString() %>
                    </span></h1>
                <% } %>
        </div>

    </div>
</body>

</html>